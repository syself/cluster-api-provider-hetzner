apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
kind: HetznerCluster
metadata:
  name: "${CLUSTER_NAME}"
spec:
  controlPlaneLoadBalancer:
    enabled: false
  controlPlaneEndpoint:
    host: "my.test.host"
    port: 6443
